services:
  ultron:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ultron-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
